import { Carousel, CarouselContent } from "@/components/ui/carousel";

export const Promotion = () => {
  return (
    <Carousel>
      <CarouselContent>
        
      </CarouselContent>
    </Carousel>
  );
};
